export function somar (x: number, y: number): number {
    return x + y;
}

export function subtrai (x: number, y: number): number {
    return x - y;
}

export function multiplicar (x: number, y: number): number {
    return x * y;
}

